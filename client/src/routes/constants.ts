export const PRIVATE_ROUTES = {
    HOME: '/',
};

export const AUTH_ROUTES = {
    SIGNIN: '/signin',
    SIGNUP: '/signup',
    ADD_PHONE: '/add-phone',
};

export const PUBLIC_ROUTES = {
    PAGE_NOT_FOUND: '*',
}